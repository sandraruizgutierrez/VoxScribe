// Configuration for extension
// Change this URL to match your server

// For local development:
const SERVER_URL = 'http://localhost:3000';

// For production (Vercel):
// const SERVER_URL = 'https://voxscribe.vercel.app';

// If using Vercel, make sure the /api/transcribe endpoint is properly deployed
// Otherwise, comment it out and uncomment localhost above

export { SERVER_URL };
